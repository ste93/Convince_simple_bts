<?xml version="1.0"?>
<root main_tree_to_execute="BehaviorTree">
    <!-- ////////// -->
    <BehaviorTree ID="BehaviorTree">
        <ReactiveSequence>
            <ReactiveFallback>
                <Condition ID="YARPCondition" carrier="tcp" name="BatteryLevel" port_name="BatteryLevel"/>
                <Action ID="AlwaysRunning" name="Alarm"/>
            </ReactiveFallback>
            <Action ID="YARPAction" carrier="tcp" name="BatteryDrainer" port_name="BatteryDrainer"/>
        </ReactiveSequence>
    </BehaviorTree>
    <!-- ////////// -->
    <TreeNodesModel>
        <Action ID="AlwaysRunning"/>
        <Action ID="YARPAction">
            <input_port name="carrier" type="std::string"/>
            <input_port name="port_name" type="std::string"/>
        </Action>
        <Condition ID="YARPCondition">
            <input_port name="carrier" type="std::string"/>
            <input_port name="port_name" type="std::string"/>
        </Condition>
    </TreeNodesModel>
    <!-- ////////// -->
</root>

